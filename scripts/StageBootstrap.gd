extends Node3D

## Build-in-Code-Wurzel von "The Little War Show" (Konventionen §3).
## Die gesamte Bühne (Geometrie, Licht, Environment, Kamera, Figuren, Props,
## Projektor) wird zur Laufzeit hier aufgebaut.
##
## Schritt 00: nur Stub — leeres, fehlerfrei laufendes Projekt. Der eigentliche
## Aufbau beginnt in Schritt 01 (Bühnen-Diorama).

func _ready() -> void:
	print("stage ready")
