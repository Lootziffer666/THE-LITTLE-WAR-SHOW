// STUB — implemented by the backstage module. Owns: wings, dressing/staff room,
// office, restrooms, and the storage room with covered (dust-sheeted) unused
// scenery, behind the stage.
export function Backstage() {
  return <group name="Backstage" />
}
