// STUB — implemented by the console module. Owns: the diegetic lighting/FX desk
// (spring-loaded faders & switches) that selects and travels the stage props,
// switches lighting states, works the curtain and fires the pyro.
export function TechConsole() {
  return <group name="TechConsole" />
}
