// STUB — implemented by the seating module. Renders nothing until built so the
// build stays green. Owns: raked rows of velour fold-down seats (some stuck
// half-up), aisle jump-seats, end standards.
export function Seating() {
  return <group name="Seating" />
}
